class Counter {

    Val number;
    Array<String, 2> array;

    Val main() {
        Morpion morpion = Morpion.new();
        morpion.play();
        return 0;
    }

    Bool testBlocks(Val v) {
        if v.equalsZero() {
            return Bool.true();
        };
        return Bool.false();
    }
}