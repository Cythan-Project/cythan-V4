class Counter {

    Val number;
    Array<String, N2> array;

    Val main() {
        /* Array<Byte, N3> array = ['a', 'b', 'c'];
        array.get<N1>().print();
        array.setDyn(1, 'd');
        array.getDyn(2).print(); */
        Morpion morpion = Morpion.new();
        morpion.play();
        return 0;
        /*a*/
    }

    Bool testBlocks(Val v) {
        if v.equalsZero() {
            return Bool.true();
        };
        return Bool.false();
    }
}