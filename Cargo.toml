[package]
name = "cythan-v4"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
lir = { path = "crates/lir" }
mir = { path = "crates/mir" }
cythan = { path = "crates/vm" }
cythan-driver = { path = "crates/driver" }
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
typer = { path = "crates/typer" }

[profile.release]
debug = true

[workspace]
members = ["crates/lir", "crates/vm", "crates/mir", "crates/errors", "crates/frontend", "crates/driver", "crates/parser", "crates/new_parser", "crates/typer", "crates/hir"]

[workspace.lints.clippy]
# Error type from ariadne is inherently large (192 bytes); boxing it would require
# changing every Result<T, Error> across the codebase for minimal benefit.
result_large_err = "allow"
large_enum_variant = "allow"

[lints]
workspace = true
