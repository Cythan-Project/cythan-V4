use std::collections::HashMap;

use crate::parser::ty::Type;

use super::{
    mir::{Mir, MirCodeBlock},
    state::code_manager::CodeManager,
};

/* pub struct MirCodeBlock {
    pub mir: Vec<Mir>,
} */
